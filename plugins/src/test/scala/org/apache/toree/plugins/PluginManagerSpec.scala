package org.apache.toree.plugins

import org.scalatest.{FunSpec, Matchers, OneInstancePerTest}

class PluginManagerSpec extends FunSpec with Matchers with OneInstancePerTest {
  describe("PluginManager") {
    fail()
  }
}
